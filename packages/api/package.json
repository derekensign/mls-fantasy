{
    "name": "@mls-fantasy/api",
    "version": "1.0.0",
    "description": "Shared API client for MLS Fantasy, including AWS DynamoDB client and Axios API calls.",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
      "clean": "rm -rf dist",
      "build": "npm run clean && tsc -p tsconfig.json",
      "watch": "tsc -p tsconfig.json --watch"
    },
    "dependencies": {
      "axios": "^1.6.7",
      "@aws-sdk/client-dynamodb": "^3.523.0",
      "@aws-sdk/lib-dynamodb": "^3.523.0",
      "dotenv": "^16.4.5"
    },
    "devDependencies": {
      "typescript": "^5"
    }
  }
  